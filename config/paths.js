const path = require("path");

module.exports = {
  entryDir: path.join(__dirname, "..", "src", "scripts", "entry"),
  distDir: path.join(__dirname, "..", "dist"),
  webpageDir: path.join(__dirname, "..", "src", "webpages"),
}
